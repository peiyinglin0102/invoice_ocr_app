{
  "name": "invoice_ocr_app",
  "version": "1.0.0",
  "description": "Invoice OCR Web App",
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "postinstall": "npm run install:frontend",
    "start": "npm run dev",
    "dev": "npx concurrently -n \"backend,frontend\" -c \"blue,green\" \"cd backend && (if not exist venv\\Scripts\\activate.bat python -m venv venv) && call venv\\Scripts\\activate.bat && pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 8000 --reload\" \"cd frontend && npm run dev\""
  }
}
